---
- hosts: all
  remote_user: usiusi
  name: Docker Installation
  roles:
  - docker_install
  tags:
    docker_install

- hosts: all
  remote_user: usiusi
  name: Transfer zip files
  roles:
  - transfer_files


- hosts: all
  remote_user: usiusi
  name: Container Setup
  roles:
  - container_setup
